# ============================================================
# Math LLM — Python dependencies
# ============================================================
# Core deep-learning stack
torch==2.2.2

# Dataset access
# The real dataset (DeepMind Mathematics Dataset, Saxton et al. 2019) is
# downloaded directly from its official static-file source
# (storage.googleapis.com) using only the Python standard library
# (urllib, tarfile) — see dataset.py. No HuggingFace `datasets` /
# `huggingface_hub` dependency is needed, which also removes a whole class
# of Colab package-version conflicts those libraries used to cause.

# Numerics / science
numpy==1.26.4
scipy==1.13.1

# Experiment tracking & visualisation
matplotlib==3.9.0
seaborn==0.13.2
tensorboard==2.17.0           # optional but recommended for loss curves

# Progress bars
tqdm==4.66.4

# Config & serialisation
pyyaml==6.0.2
jsonlines==4.0.0

# Notebook support (local)
jupyter==1.0.0
ipywidgets==8.1.3

# Utilities
colorlog==6.8.2               # coloured console logging
tabulate==0.9.0               # pretty-print tables in evaluation reports
